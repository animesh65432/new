import React from 'react'

type props = {
    image: string,
    title: string,
    summary: string,
    Episode: string
}
const PodcastCard: React.FC<props> = ({ image, title, summary, Episode }) => {
    return (
        <div></div>
    )
}

export default PodcastCard